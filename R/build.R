# An optional custom script to run before Hugo builds your site.
# You can delete it if you do not need it.

library(blogdown)
##template download
# blogdown::new_site(theme = "opera7133/tella")

##build and serve site locally
# blogdown::build_site()
# blogdown::serve_site()
